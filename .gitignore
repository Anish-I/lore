# dependencies
node_modules/
desktop/node_modules/
.venv/
*.egg-info/

# secrets & local config (never commit)
.env
.env.*
*.token
.conda/
secrets/

# python
__pycache__/
*.pyc
.pytest_cache/

# data stores & runtime state
.qdrant/
.pgdata/
.pgdata-mac-test/
data/
logs/
*.log
.omx/
.lore/

# scenario eval: generated datasets + isolated work stores (regenerable —
# see eval/scenarios/gen_scenario.py; insurance.json alone is ~20MB)
eval/scenarios/data/
eval/scenarios/.work/

# build artifacts
dist/
build/
desktop/dist/

# editor / tooling
.cursor/
.idea/
.vscode/
*.stackdump

# test artifacts
desktop/e2e-shot-*.png
client_secret_*.json
*google_oauth_client*.json

# superpowers SDD scratch (ledger, briefs, diffs)
.superpowers/

# PyInstaller backend freeze artifacts
core/pyi-dist/
core/pyi-build/
core/pyi-spec/
*.spec

# local store artifacts (ruvector / rvf)
ruvector.db
*.rvf
*.rvf.lock
